<% const activePage = 'courses'; const pageTitle = 'Chỉnh sửa khóa học'; %>
<%- include('../common/header', {user, activePage, pageTitle}) %>

<div class="adm-page-header">
  <div>
    <div class="adm-page-title">Chỉnh sửa khóa học</div>
    <div class="adm-page-subtitle"><a href="/admin/course/coursesList" style="color:#2563eb">← Quay lại danh sách</a></div>
  </div>
</div>

<% if (data.course_info) { %>
<form action="" method="post" id="frmEdit">
  <input type="hidden" name="id"         value="<%= data.course_info._id %>" />
  <input type="hidden" name="lecture_id" value="<%= user._id %>" />

  <!-- Tabs -->
  <div style="display:flex;gap:4px;margin-bottom:16px">
    <button type="button" class="tab-btn active" data-tab="info"     onclick="switchTab('info')">Thông tin</button>
    <button type="button" class="tab-btn"         data-tab="sessions" onclick="switchTab('sessions')">Buổi học</button>
    <button type="button" class="tab-btn"         data-tab="discount" onclick="switchTab('discount')">Mã giảm giá</button>
  </div>

  <style>
  .tab-btn {
    padding:8px 18px;border:1px solid rgba(255,255,255,0.10);border-radius:8px;
    background:transparent;color:#64748b;font-size:13px;font-weight:600;cursor:pointer;
    font-family:inherit;transition:all 0.15s;
  }
  .tab-btn.active { background:#2563eb;border-color:#2563eb;color:#fff; }
  .tab-pane { display:none; }
  .tab-pane.active { display:block; }
  </style>

  <!-- ── Tab: Thông tin ── -->
  <div class="tab-pane active" id="tab-info">
    <div style="display:grid;grid-template-columns:1fr 360px;gap:16px;align-items:start">

      <div style="display:flex;flex-direction:column;gap:16px">
        <div class="adm-card">
          <div class="adm-card-header"><span class="adm-card-title"><i class="fas fa-info-circle" style="color:#2563eb;margin-right:8px"></i>Thông tin cơ bản</span></div>
          <div class="adm-card-body">
            <div class="adm-form-grid">
              <div class="adm-field adm-field-full">
                <label>Tên khóa học</label>
                <input type="text" name="name" value="<%= data.course_info.name %>" />
              </div>
              <div class="adm-field">
                <label>Danh mục</label>
                <select name="category">
                  <% data.categories && data.categories.forEach(cat => { %>
                    <option value="<%= cat._id %>" <%= data.course_info.idCourseTopic && data.course_info.idCourseTopic.toString() === cat._id.toString() ? 'selected' : '' %>>
                      <%= cat.name %>
                    </option>
                  <% }) %>
                </select>
              </div>
              <div class="adm-field">
                <label>Trạng thái</label>
                <select name="status">
                  <option value="1" <%= data.course_info.status ? 'selected' : '' %>>Hiển thị</option>
                  <option value="0" <%= !data.course_info.status ? 'selected' : '' %>>Ẩn</option>
                </select>
              </div>
              <div class="adm-field adm-field-full">
                <label>Mô tả</label>
                <textarea name="description" rows="4"><%= data.course_info.description || '' %></textarea>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div style="display:flex;flex-direction:column;gap:16px">
        <!-- Poster -->
        <div class="adm-card">
          <div class="adm-card-header"><span class="adm-card-title"><i class="fas fa-image" style="color:#d97706;margin-right:8px"></i>Poster</span></div>
          <div class="adm-card-body">
            <div id="poster-zone" style="position:relative;border-radius:8px;overflow:hidden;cursor:pointer;background:#0f172a;margin-bottom:10px" onclick="document.getElementById('poster-file').click()">
              <img id="poster-img" src="<%= data.course_info.poster || '' %>" style="width:100%;display:block;object-fit:cover;max-height:160px;background:#0f172a" />
              <div class="poster-overlay" style="position:absolute;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(0,0,0,0.5);color:#fff;font-size:13px;font-weight:600;gap:6px;opacity:0;transition:opacity 0.2s">
                <i class="fas fa-camera"></i> Đổi poster
              </div>
              <div class="poster-spinner" style="position:absolute;inset:0;display:none;align-items:center;justify-content:center;background:rgba(15,23,42,0.78);color:#93c5fd;font-size:13px;font-weight:600;gap:8px">
                <i class="fas fa-circle-notch fa-spin"></i> Đang tải lên...
              </div>
            </div>
            <input type="file" id="poster-file" style="display:none" accept="image/*" />
            <input type="hidden" name="image" id="poster-url" value="<%= data.course_info.poster || '' %>" />
            <button type="button" class="adm-btn adm-btn-ghost" style="width:100%;height:34px;font-size:12px" onclick="document.getElementById('poster-file').click()">
              <i class="fas fa-upload"></i> Đổi poster
            </button>
            <div id="poster-error" style="font-size:12px;color:#f87171;margin-top:6px;min-height:14px"></div>
          </div>
        </div>

        <style>
        #poster-zone:hover .poster-overlay { opacity:1; }
        #poster-zone.uploading .poster-spinner { display:flex !important; }
        #poster-zone.uploading { pointer-events:none; }
        </style>

        <!-- Giá -->
        <div class="adm-card">
          <div class="adm-card-header"><span class="adm-card-title"><i class="fas fa-tag" style="color:#16a34a;margin-right:8px"></i>Giá</span></div>
          <div class="adm-card-body">
            <div style="display:flex;gap:8px;margin-bottom:12px">
              <label style="display:flex;align-items:center;gap:6px;cursor:pointer;padding:6px 12px;border-radius:8px;border:1.5px solid rgba(255,255,255,0.10);background:#0f172a;transition:all 0.15s;white-space:nowrap" id="edit-lbl-fixed">
                <input type="radio" name="priceType" value="fixed" <%= (!data.course_info.priceType || data.course_info.priceType === 'fixed') ? 'checked' : '' %> onchange="editTogglePrice(this.value)" style="width:14px;height:14px;accent-color:#2563eb;flex-shrink:0" />
                <span style="font-size:12px;font-weight:600;color:#e2e8f0">💰 Hiển thị giá</span>
              </label>
              <label style="display:flex;align-items:center;gap:6px;cursor:pointer;padding:6px 12px;border-radius:8px;border:1.5px solid rgba(255,255,255,0.10);background:#0f172a;transition:all 0.15s;white-space:nowrap" id="edit-lbl-contact">
                <input type="radio" name="priceType" value="contact" <%= data.course_info.priceType === 'contact' ? 'checked' : '' %> onchange="editTogglePrice(this.value)" style="width:14px;height:14px;accent-color:#2563eb;flex-shrink:0" />
                <span style="font-size:12px;font-weight:600;color:#e2e8f0">📞 Liên hệ riêng</span>
              </label>
            </div>
            <div style="margin-bottom:12px">
              <div style="font-size:11px;font-weight:600;color:#64748b;margin-bottom:8px">Loại khóa học</div>
              <div style="display:flex;gap:8px">
                <label style="display:flex;align-items:center;gap:6px;cursor:pointer;padding:6px 12px;border-radius:8px;border:1.5px solid rgba(255,255,255,0.10);background:#0f172a;transition:all 0.15s;white-space:nowrap;flex:1;justify-content:center" id="edit-lbl-course-session">
                  <input type="radio" name="courseType" value="session" <%= (!data.course_info.courseType || data.course_info.courseType === 'session') ? 'checked' : '' %> onchange="editToggleCourseType(this.value)" style="width:14px;height:14px;accent-color:#2563eb;flex-shrink:0" />
                  <span style="font-size:12px;font-weight:600;color:#e2e8f0">📅 Theo buổi</span>
                </label>
                <label style="display:flex;align-items:center;gap:6px;cursor:pointer;padding:6px 12px;border-radius:8px;border:1.5px solid rgba(255,255,255,0.10);background:#0f172a;transition:all 0.15s;white-space:nowrap;flex:1;justify-content:center" id="edit-lbl-course-hour">
                  <input type="radio" name="courseType" value="hour" <%= data.course_info.courseType === 'hour' ? 'checked' : '' %> onchange="editToggleCourseType(this.value)" style="width:14px;height:14px;accent-color:#2563eb;flex-shrink:0" />
                  <span style="font-size:12px;font-weight:600;color:#e2e8f0">⏱️ Theo giờ</span>
                </label>
              </div>
            </div>
            <div id="edit-price-wrap" style="<%= data.course_info.priceType === 'contact' ? 'display:none' : '' %>">
              <div class="adm-field">
                <label>Giá (VNĐ)</label>
                <input type="number" name="tuition" value="<%= data.course_info.tuition || 500000 %>" min="0" step="1000" />
              </div>
            </div>
            <div class="adm-field" id="edit-total-sessions-wrap" style="<%= data.course_info.courseType === 'hour' ? 'display:none;' : '' %>margin-top:14px">
              <label>Số buổi học dự kiến</label>
              <input type="number" name="totalSessions" value="<%= data.course_info.totalSessions || 0 %>" min="0" placeholder="VD: 12" />
              <div style="font-size:11px;color:#64748b;line-height:1.5">Dùng để tính tiến độ học của học viên (số buổi đã hoàn thành / tổng buổi)</div>
            </div>
            <div class="adm-field" id="edit-total-hours-wrap" style="<%= data.course_info.courseType === 'hour' ? '' : 'display:none;' %>margin-top:14px">
              <label>Số giờ học dự kiến</label>
              <input type="number" name="totalHours" value="<%= data.course_info.totalHours || 0 %>" min="0" step="0.5" placeholder="VD: 24" />
              <div style="font-size:11px;color:#64748b;line-height:1.5">Dùng cho khóa học tính theo giờ học</div>
            </div>
            <div id="edit-contact-note" style="<%= data.course_info.priceType !== 'contact' ? 'display:none' : '' %>;background:#0f172a;border:1px solid rgba(255,255,255,0.08);border-radius:8px;padding:12px 14px;font-size:13px;color:#94a3b8">
              <i class="fas fa-info-circle" style="color:#3b6fd4;margin-right:6px"></i>
              Học viên sẽ thấy nút "Liên hệ" thay vì giá tiền.
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- ── Tab: Buổi học ── -->
  <div class="tab-pane" id="tab-sessions">
    <div class="adm-card">
      <div class="adm-card-header">
        <span class="adm-card-title"><i class="fas fa-link" style="color:#2563eb;margin-right:8px"></i>Danh sách buổi học</span>
        <button type="button" class="adm-btn adm-btn-primary adm-btn-sm" onclick="addSession()">
          <i class="fas fa-plus"></i> Thêm buổi
        </button>
      </div>
      <div class="adm-card-body">
        <div id="session-list" style="display:flex;flex-direction:column;gap:10px">
          <% (data.course_info.sessions || []).forEach((s, i) => { %>
          <div class="session-row" id="sr-<%= i %>" style="background:#0f172a;border:1px solid rgba(255,255,255,0.08);border-radius:10px;padding:14px;display:grid;grid-template-columns:1fr 1fr auto;gap:10px;align-items:start">
            <div class="adm-field">
              <label>Tiêu đề buổi học</label>
              <input type="text" name="sessions[<%= i %>][title]" value="<%= s.title || '' %>" placeholder="VD: Buổi 1 — Kỹ thuật dribble" />
            </div>
            <div class="adm-field">
              <label>Link buổi học</label>
              <input type="url" name="sessions[<%= i %>][link]" value="<%= s.link || '' %>" placeholder="https://meet.google.com/..." />
            </div>
            <button type="button" onclick="document.getElementById('sr-<%= i %>').remove()" style="background:none;border:none;color:#f87171;cursor:pointer;font-size:16px;padding:4px;margin-top:20px"><i class="fas fa-trash"></i></button>
            <div class="adm-field" style="grid-column:1/-1">
              <label>Ghi chú</label>
              <input type="text" name="sessions[<%= i %>][note]" value="<%= s.note || '' %>" placeholder="Nội dung buổi học, yêu cầu chuẩn bị..." />
            </div>
          </div>
          <% }) %>
        </div>
        <% if (!data.course_info.sessions || data.course_info.sessions.length === 0) { %>
        <div id="session-empty" style="text-align:center;padding:32px;color:#475569">
          <i class="fas fa-link" style="font-size:28px;margin-bottom:8px;display:block"></i>
          Chưa có buổi học nào. Nhấn "+ Thêm buổi" để bắt đầu.
        </div>
        <% } %>
      </div>
    </div>
  </div>

  <!-- ── Tab: Mã giảm giá ── -->
  <div class="tab-pane" id="tab-discount">
    <div class="adm-card">
      <div class="adm-card-header">
        <span class="adm-card-title"><i class="fas fa-percent" style="color:#16a34a;margin-right:8px"></i>Mã giảm giá</span>
        <button type="button" class="adm-btn adm-btn-primary adm-btn-sm" onclick="addDiscount()">
          <i class="fas fa-plus"></i> Thêm mã
        </button>
      </div>
      <div class="adm-card-body">
        <!-- Header -->
        <div style="display:grid;grid-template-columns:2fr 70px 80px 80px 120px auto;gap:8px;padding:0 0 8px;border-bottom:1px solid rgba(255,255,255,0.07);margin-bottom:8px">
          <span style="font-size:10px;font-weight:700;color:#475569;text-transform:uppercase">Mã</span>
          <span style="font-size:10px;font-weight:700;color:#475569;text-transform:uppercase">% Giảm</span>
          <span style="font-size:10px;font-weight:700;color:#475569;text-transform:uppercase">Tổng lượt</span>
          <span style="font-size:10px;font-weight:700;color:#475569;text-transform:uppercase">Đã dùng</span>
          <span style="font-size:10px;font-weight:700;color:#475569;text-transform:uppercase">Hết hạn</span>
          <span></span>
        </div>
        <div id="discount-list" style="display:flex;flex-direction:column;gap:8px">
          <% (data.course_info.discountCodes || []).forEach((dc, i) => {
            const remaining = dc.maxUses === 0 ? '∞' : Math.max(0, dc.maxUses - (dc.usedCount || 0));
            const isExhausted = dc.maxUses > 0 && (dc.usedCount || 0) >= dc.maxUses;
            const isExpired   = dc.expiresAt && new Date() > new Date(dc.expiresAt);
            const statusOk    = !isExhausted && !isExpired && dc.active;
          %>
          <div id="dc-<%= i %>" style="display:grid;grid-template-columns:2fr 70px 80px 80px 120px auto;gap:8px;align-items:center;padding:8px;border-radius:8px;background:<%= statusOk ? 'transparent' : 'rgba(220,38,38,0.06)' %>;border:1px solid <%= statusOk ? 'transparent' : 'rgba(220,38,38,0.15)' %>">
            <div style="position:relative">
              <input type="text" name="discountCodes[<%= i %>][code]" value="<%= dc.code %>" class="dc-input" style="<%= statusOk ? '' : 'padding-right:80px' %>" />
              <% if (!statusOk) { %>
              <span style="position:absolute;right:6px;top:50%;transform:translateY(-50%);font-size:10px;font-weight:700;padding:2px 6px;border-radius:4px;background:rgba(220,38,38,0.15);color:#f87171;white-space:nowrap;pointer-events:none">
                <%= isExpired ? 'Hết hạn' : isExhausted ? 'Hết lượt' : 'Tắt' %>
              </span>
              <% } %>
            </div>
            <input type="number" name="discountCodes[<%= i %>][percent]" value="<%= dc.percent %>" class="dc-input" min="1" max="100" required />
            <input type="number" name="discountCodes[<%= i %>][maxUses]" value="<%= dc.maxUses %>" class="dc-input" min="0" title="0 = không giới hạn" />
            <!-- Đã dùng — readonly -->
            <div style="height:34px;padding:0 10px;background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.07);border-radius:6px;display:flex;align-items:center;justify-content:center;font-size:13px;color:<%= isExhausted ? '#f87171' : '#94a3b8' %>;font-weight:600">
              <%= dc.usedCount || 0 %>
              <% if (dc.maxUses > 0) { %><span style="color:#475569;font-weight:400;margin-left:4px">/ <%= dc.maxUses %></span><% } %>
            </div>
            <input type="date" name="discountCodes[<%= i %>][expiresAt]" value="<%= dc.expiresAt ? new Date(dc.expiresAt).toISOString().split('T')[0] : '' %>" class="dc-input" />
            <button type="button" onclick="document.getElementById('dc-<%= i %>').remove()" style="background:none;border:none;color:#f87171;cursor:pointer;font-size:14px"><i class="fas fa-times"></i></button>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </div>

  <div style="display:flex;justify-content:flex-end;gap:10px;margin-top:16px">
    <button type="button" class="adm-btn adm-btn-ghost" onclick="location.href='/admin/course/coursesList'">Huỷ</button>
    <button type="submit" class="adm-btn adm-btn-primary"><i class="fas fa-save"></i> Lưu thay đổi</button>
  </div>
</form>
<% } %>

<style>
.dc-input {
  height:34px;padding:0 10px;background:#0f172a;
  border:1px solid rgba(255,255,255,0.10);border-radius:6px;
  color:#f1f5f9;font-size:12px;outline:none;width:100%;
  box-sizing:border-box;font-family:inherit;
  text-align:center !important;
}
.dc-input:focus { border-color:#2563eb; }
.dc-input::placeholder { text-align:center; }
/* Ẩn spinner để text canh giữa thật sự — Chrome/Edge/Safari */
.dc-input[type=number]::-webkit-outer-spin-button,
.dc-input[type=number]::-webkit-inner-spin-button {
  -webkit-appearance: none; margin: 0;
}
/* Firefox */
.dc-input[type=number] { -moz-appearance: textfield; }
</style>

<script>
// ── Tabs ──
function switchTab(name) {
  document.querySelectorAll('.tab-pane').forEach(p => p.classList.remove('active'));
  document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
  document.getElementById('tab-' + name).classList.add('active');
  document.querySelector('[data-tab="' + name + '"]').classList.add('active');
}

// ── Thêm buổi học ──
let sessionCount = <%= (data.course_info.sessions || []).length %>;
function addSession() {
  const empty = document.getElementById('session-empty');
  if (empty) empty.remove();
  const i = sessionCount++;
  const html = `
    <div class="session-row" id="sr-${i}" style="background:#0f172a;border:1px solid rgba(255,255,255,0.08);border-radius:10px;padding:14px;display:grid;grid-template-columns:1fr 1fr auto;gap:10px;align-items:start">
      <div class="adm-field">
        <label>Tiêu đề buổi học</label>
        <input type="text" name="sessions[${i}][title]" placeholder="VD: Buổi ${i+1} — Kỹ thuật dribble" />
      </div>
      <div class="adm-field">
        <label>Link buổi học</label>
        <input type="url" name="sessions[${i}][link]" placeholder="https://meet.google.com/..." />
      </div>
      <button type="button" onclick="document.getElementById('sr-${i}').remove()" style="background:none;border:none;color:#f87171;cursor:pointer;font-size:16px;padding:4px;margin-top:20px"><i class="fas fa-trash"></i></button>
      <div class="adm-field" style="grid-column:1/-1">
        <label>Ghi chú</label>
        <input type="text" name="sessions[${i}][note]" placeholder="Nội dung buổi học, yêu cầu chuẩn bị..." />
      </div>
    </div>`;
  document.getElementById('session-list').insertAdjacentHTML('beforeend', html);
}

// ── Thêm mã giảm giá ──
let discountCount = <%= (data.course_info.discountCodes || []).length %>;
function addDiscount() {
  const i = discountCount++;
  const html = `
    <div id="dc-${i}" style="display:grid;grid-template-columns:2fr 70px 80px 80px 120px auto;gap:8px;align-items:center;padding:8px;border-radius:8px;background:transparent;border:1px solid transparent">
      <div style="position:relative">
        <input type="text" name="discountCodes[${i}][code]" placeholder="SALE20" class="dc-input" oninput="this.value=this.value.toUpperCase()" />
      </div>
      <input type="number" name="discountCodes[${i}][percent]"   placeholder="%"       class="dc-input" min="1" max="100" required />
      <input type="number" name="discountCodes[${i}][maxUses]"   placeholder="0=∞"     class="dc-input" min="0" title="0 = không giới hạn" />
      <div style="height:34px;padding:0 10px;background:rgba(255,255,255,0.04);border:1px solid rgba(255,255,255,0.07);border-radius:6px;display:flex;align-items:center;justify-content:center;font-size:13px;color:#475569">0</div>
      <input type="date"   name="discountCodes[${i}][expiresAt]"                        class="dc-input" />
      <button type="button" onclick="document.getElementById('dc-${i}').remove()" style="background:none;border:none;color:#f87171;cursor:pointer;font-size:14px"><i class="fas fa-times"></i></button>
    </div>`;
  document.getElementById('discount-list').insertAdjacentHTML('beforeend', html);
}

// ── Toggle price type (edit) ──
function editTogglePrice(val) {
  const wrap    = document.getElementById('edit-price-wrap');
  const note    = document.getElementById('edit-contact-note');
  const lblF    = document.getElementById('edit-lbl-fixed');
  const lblC    = document.getElementById('edit-lbl-contact');
  if (val === 'contact') {
    wrap.style.display = 'none'; note.style.display = '';
    lblC.style.borderColor = '#2563eb'; lblF.style.borderColor = 'rgba(255,255,255,0.10)';
  } else {
    wrap.style.display = ''; note.style.display = 'none';
    lblF.style.borderColor = '#2563eb'; lblC.style.borderColor = 'rgba(255,255,255,0.10)';
  }
}
// Set border màu đúng khi load
(function() {
  const isContact = '<%= data.course_info.priceType %>' === 'contact';
  const lblF = document.getElementById('edit-lbl-fixed');
  const lblC = document.getElementById('edit-lbl-contact');
  if (lblF && lblC) {
    lblF.style.borderColor = isContact ? 'rgba(255,255,255,0.10)' : '#2563eb';
    lblC.style.borderColor = isContact ? '#2563eb' : 'rgba(255,255,255,0.10)';
  }
})();
// ── Upload poster → ImgBB ──
function editToggleCourseType(val) {
  const sessionWrap = document.getElementById('edit-total-sessions-wrap');
  const hourWrap = document.getElementById('edit-total-hours-wrap');
  const lblSession = document.getElementById('edit-lbl-course-session');
  const lblHour = document.getElementById('edit-lbl-course-hour');
  if (val === 'hour') {
    sessionWrap.style.display = 'none';
    hourWrap.style.display = '';
    lblHour.style.borderColor = '#2563eb';
    lblSession.style.borderColor = 'rgba(255,255,255,0.10)';
  } else {
    sessionWrap.style.display = '';
    hourWrap.style.display = 'none';
    lblSession.style.borderColor = '#2563eb';
    lblHour.style.borderColor = 'rgba(255,255,255,0.10)';
  }
}
(function() {
  const isHour = '<%= data.course_info.courseType %>' === 'hour';
  const lblSession = document.getElementById('edit-lbl-course-session');
  const lblHour = document.getElementById('edit-lbl-course-hour');
  if (lblSession && lblHour) {
    lblSession.style.borderColor = isHour ? 'rgba(255,255,255,0.10)' : '#2563eb';
    lblHour.style.borderColor = isHour ? '#2563eb' : 'rgba(255,255,255,0.10)';
  }
})();
const IMGBB_KEY = '<%= process.env.IMGBB_API_KEY %>';
let _posterUploading = false;

document.getElementById('poster-file').addEventListener('change', async function() {
  const file = this.files[0];
  if (!file) return;

  const errEl = document.getElementById('poster-error');
  const zone  = document.getElementById('poster-zone');
  errEl.textContent = '';

  if (!file.type.startsWith('image/')) {
    errEl.textContent = '✗ File phải là ảnh';
    this.value = '';
    return;
  }
  if (file.size > 10 * 1024 * 1024) {
    errEl.textContent = '✗ Ảnh quá lớn (>10MB)';
    this.value = '';
    return;
  }

  // Preview ngay
  const reader = new FileReader();
  reader.onload = e => { document.getElementById('poster-img').src = e.target.result; };
  reader.readAsDataURL(file);

  // Upload ImgBB
  zone.classList.add('uploading');
  _posterUploading = true;

  const fd = new FormData();
  fd.append('image', file);
  try {
    const res  = await fetch('https://api.imgbb.com/1/upload?key=' + IMGBB_KEY, { method:'POST', body:fd });
    const json = await res.json();
    if (json.success && json.data && json.data.url) {
      document.getElementById('poster-img').src = json.data.url;
      document.getElementById('poster-url').value = json.data.url;
      if (typeof admToast === 'function') admToast('Poster đã cập nhật', 'success');
    } else {
      errEl.textContent = '✗ Upload thất bại: ' + (json.error?.message || 'không rõ lỗi');
    }
  } catch(e) {
    errEl.textContent = '✗ Lỗi kết nối ImgBB';
    console.error('poster upload error', e);
  } finally {
    zone.classList.remove('uploading');
    _posterUploading = false;
    this.value = '';
  }
});

// Chặn submit khi đang upload
document.getElementById('frmEdit').addEventListener('submit', (ev) => {
  if (_posterUploading) {
    ev.preventDefault();
    document.getElementById('poster-error').textContent = '⏳ Ảnh đang tải lên, vui lòng chờ...';
    return false;
  }
});
</script>

<%- include('../common/footer') %>
